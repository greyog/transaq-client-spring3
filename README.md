Create .env file at project root with the following aliases:
```
TRANSAQ_LOGIN=<your login>
TRANSAQ_PASSWORD=<your pass>
TRANSAQ_HOST=<finam trade host>

```

Before main app you should launch docker-compose.yml.